<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.myapplication">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".GirisEkrani">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".KayitOlActivity"
            android:label="Kayıt Ol"/>
        <activity android:name=".OturumAcActivity"
            android:label="Oturum Aç"/>
        <activity android:name=".GonulluAnaEkrani"
            android:label="Gönüllü Ana Ekranı" />
        <activity android:name=".GormeEngelliAnaEkrani"
            android:label="Görme Engelli Ana Ekranı" />
        <activity android:name=".GonulluKullaniciEkrani"
            android:label="Gönüllü Kullanıcı Ekranı" />
        <activity android:name=".GormeEngelliKullaniciEkrani"
            android:label="Görme Engelli Kullanıcı Ekranı" />
        <activity android:name=".AyarlarEkrani"
        android:label="Ayarlar"/>
        <activity android:name=".ProfilDuzenlemeEkrani"
            android:label="Profili Düzenle"/>
        <activity android:name=".deneme"
            android:label="Linear Layout Deneme"/>
    </application>

</manifest>